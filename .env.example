# Copy to .env and adjust if needed. Variables are read from the process environment;
# Django does not load this file automatically.

SECRET_KEY=change-me
DEBUG=true
ALLOWED_HOSTS=127.0.0.1,localhost

# DB_ENGINE=django.db.backends.sqlite3
# DB_NAME=db.sqlite3

# API_TITLE=Order Promocode Service
# API_VERSION=1.0.0
